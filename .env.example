# Cổng publish ra host (chỉ dùng khi cloudflared chạy ngoài Docker)
WEB_PORT=8080

# Token của Cloudflare Tunnel (Zero Trust > Networks > Tunnels > Configure)
# Chỉ cần khi chạy profile "tunnel"
TUNNEL_TOKEN=

# Vite inline biến này lúc build (hiện code chưa dùng, để trống được)
GEMINI_API_KEY=
